{% extends "master.stencil" %}

{% block body %}
    {% for story in stories %}
        <h2><a href="{{ story.id }}">{{ story.title }}</a></h2>
        <p>{{ story.strap }}</p>
    {% endfor %}
{% endblock %}
